# Courses Tab UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the admin Courses tab feel sleeker while staying consistent with the existing site design.

**Architecture:** Keep the existing static React page and CSS token system. Improve only the Courses tab markup, styling, generated build artifacts, and one focused render test.

**Tech Stack:** React via `app.jsx`, CSS in `styles.css`, Node test/build scripts.

## Global Constraints

- No new dependencies.
- No new color palette.
- No marketing-only sections.
- Preserve keyboard focus and native button semantics.
- Keep motion limited to hover/focus micro-interactions.

---

### Task 1: Courses Render Contract

**Files:**
- Modify: `tests/prerender.test.mjs`

**Interfaces:**
- Consumes: generated `index.html` from `npm run build`.
- Produces: a render assertion that the Courses tab contains the new proof strip and curriculum shell.

- [ ] **Step 1: Add failing test**

```js
test('prerendered courses page includes polished course proof and curriculum shell', () => {
  const indexHtml = readFileSync(new URL('../index.html', import.meta.url), 'utf8');
  assert.match(indexHtml, /courses-proof/);
  assert.match(indexHtml, /courses-curriculum-shell/);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test tests/prerender.test.mjs`
Expected: FAIL because `courses-proof` and `courses-curriculum-shell` do not exist yet.

### Task 2: Courses UI Polish

**Files:**
- Modify: `app.jsx`
- Modify: `styles.css`
- Generated by build: `app.build.js`, `index.html`, `roadmap.html`

**Interfaces:**
- Consumes: `window.COURSE_INFO`, `window.COURSE_CURRICULUM`, existing `V2SectionHeader`, existing theme tokens.
- Produces: `courses-proof`, `courses-curriculum-shell`, and polished module/submodule/lesson styling.

- [ ] **Step 1: Patch `app.jsx`**

Add a compact `courses-proof` strip after the hero stats and wrap curriculum stats/list in `courses-curriculum-shell`.

- [ ] **Step 2: Patch `styles.css`**

Add the proof strip, shell, responsive lesson layout, reduced-motion guard, and stronger card/surface treatment using existing CSS tokens.

- [ ] **Step 3: Rebuild generated files**

Run: `npm run build`
Expected: exit 0 and updates to generated artifacts.

- [ ] **Step 4: Run verification**

Run: `npm test`
Expected: exit 0.
